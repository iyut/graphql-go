schema{
	query: Query
	mutation: Mutation
}

type User{
	userID : ID!
	username: String!
	email : String!
	posts : [Post!]!
}

type Post{
	postID: ID!
	title: String!
	postTitle: String!
}

type Query{
	users: [User!]!
	user(userID: ID!): User!
	posts(userID: ID!): [Post!]!
	post(postID: ID!): Post!
}

input PostInput{
	title: String!
}

type Mutation{
	createPost(userID: ID!, post: PostInput!): Post!
}