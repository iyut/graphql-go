schema{
	query: Query
	mutation: Mutation
}

type User{
	userID : ID!
	username: String!
	email : String!
	posts : [Post!]!
}

type Post{
	postID: ID!
	title: String!
}

type Query{
	users: [User!]!
	user(userID: ID!): User!
	posts(userID: ID!): [Post!]!
	post(postID: ID!): Post!
}

input PostInput{
	data: String!
}

type Mutation{
	createNote(userID: ID!, post: PostInput!): Post!
}